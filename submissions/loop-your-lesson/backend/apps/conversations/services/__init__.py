# AI conversation system
#
# agent.py      — while-loop agent (no framework), SSE streaming
# tools.py      — PreplyTool base class, dual-return (message, data)
# modes.py      — daily_briefing + student_practice definitions
# context.py    — LessonContext, StudentContext, PedagogicalContext injection
# query_tools/  — read-only tools that query skill outputs
#
# See: docs/conversational-ux.md
